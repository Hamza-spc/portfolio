import React from 'react';

export const CSharpIcon: React.FC<{ className?: string }> = ({ className }) => (
  <span className={className} style={{ fontFamily: 'monospace', fontWeight: 'bold' }}>
    C#
  </span>
);
